baseURL = "https://fetwtgrah.github.io/"
locale = "zh-cn"
title = "Fresh的博客"

theme = "PaperMod"

# Longer .Summary so the lead note on the homepage has something to show.
summaryLength = 40

# Custom styles live in assets/css/extended/ — PaperMod concatenates every
# .css file there in path order, hence the 00- … 60- prefixes.

[markup.goldmark.renderer]
  unsafe = true

[menu]

[[menu.main]]
name = "Home"
url = "/"
weight = 1

[[menu.main]]
name = "Posts"
url = "/posts/"
weight = 2

[[menu.main]]
name = "Projects"
url = "/projects/"
weight = 3

[[menu.main]]
name = "About"
url = "/about/"
weight = 4

[params]
  defaultTheme = "auto"
  disableThemeToggle = false

  # Mono eyebrow above the name on the homepage. Read by layouts/index.html.
  heroEyebrow = "Developer notebook"

  # Post display
  ShowReadingTime = true
  ShowShareButtons = false
  ShowCodeCopyButtons = true
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  mainSections = ["posts"]
  ShowWordCount = true
  ShowToc = true

  # Social
  socialIcons = [
    { name = "github", url = "https://github.com/fetwtgrah" },
  ]

  # Label / Logo — set in an ink block by 20-chrome.css, so the
  # terminal-style "~/" prefix would fight the frame. Plain name.
  [params.label]
    text = "Fresh"

  # Footer
  [params.footer]
    hideCopyright = false
    text = "built with Hugo · deployed on GitHub Pages"

# Hero copy. Title sets the display name, Subtitle the mono role line.
[params.homeInfoParams]
  Title = "Fresh"
  Subtitle = "HUST软件工程在读 · 后端/全栈方向"
  Content = """
  正在涨经验中...
  """

